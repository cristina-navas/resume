{
  "scripts": {
    "render-html": "npx resumed render --output docs/index.html",
    "render-pdf": "npx resumed export resume.json --output resume.pdf --theme jsonresume-theme-stackoverflow --puppeteer-arg --no-sandbox",
    "validate": "npx resumed validate resume.json",
    "prepare": "husky"
  },
  "dependencies": {
    "jsonresume-theme-even": "^0.26.1",
    "jsonresume-theme-stackoverflow": "^2.2.0",
    "resume-cli": "3.0.5",
    "resumed": "^3.0.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.4",
    "prettier": "3.6.2"
  },
  "lint-staged": {
    "**/*": "prettier resume.json --write --ignore-unknown"
  }
}
